import { Routes } from '@angular/router';
import { ClientsUploadComponent } from './clients-upload/clients-upload.component';

export const routes: Routes = [
  {
    path: '',
    component: ClientsUploadComponent,
    title: 'Categorize'
  }
];
